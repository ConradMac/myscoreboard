{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}
	{# 
	EXO : 
	Chercher une card dans boostrarp et la placer dans une boucle for xx in class et copier coller la card
	#}
	<div class=" d-flex justify-content-around flex-wrap">
		{% for koko in games %}
			{# <div class="card my-3" style="width: 18rem;">
				<div class="card-body">
					<h5 class="card-title">{{ koko.title}}</h5>
					<h6 class="card-subtitle mb-2 text-muted">Nombre de paties jouées</h6>
					<h6 class="card-subtitle mb-2 text-muted">{{koko.contests|length}}</h6>
					<p class="card-text">Entre
						{{koko.minPlayers}}
						et
						{{koko.maxPlayers}}
						joueurs
					</p>
					<a href="{{ path('app_home_contest', {'title': koko.title}) }}" class="card-link">Commencer une partie</a>
					{# <a href="#" class="card-link">Another link</a> #}
				{# </div>
				<p>
					{% for partie in koko.contests %}
						{% for joueur in partie.players %}
							{{joueur.nickname}}
						{% endfor %}
					{% endfor %}
				</p>
			</div> 
			
			
			code couper pour aller dans _card #}
		{% endfor %}
	</div>
	<hr>
	<p>Il y  a
		<strong>{{ nb_joueurs }}</strong>
		joueurs inscrits dans la base de donnée</p>
		<hr>
		<h2>Joueurs ayant déjà gagné une partie :</h2>
		<ul>
			{% for joueur in gagnants %}
			<li>{{ joueur.nickname ~ " (" ~ joueur.email ~") "}}</li>
			{% endfor %}
		</ul>

{% endblock %}

{% block piedpage %}
	<p>Footer de la page d'accueil</p>
{% endblock %}
